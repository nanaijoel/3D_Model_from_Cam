def run_depth_mvs(images_folder: str, sfm_points, out_folder: str, on_log=None, on_progress=None):
    on_log and on_log("[depth] (stub) übersprungen")
    on_progress and on_progress(97, "Depth (stub)")
